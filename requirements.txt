# Streaming weather lakehouse -- dependencies
# IMPORTANT: use Python 3.12 (pyspark 3.5 / pandas do NOT support 3.13/3.14).
pyspark==3.5.3            # Structured Streaming engine (reads Kafka)
kafka-python-ng==2.2.3    # producer (Python 3.12-compatible fork of kafka-python)
requests==2.32.3          # call the Open-Meteo API
pandas==2.2.2             # per-batch transforms + gold aggregation
sqlalchemy==2.0.51        # write to Postgres
psycopg2-binary==2.9.12   # Postgres driver
python-dotenv==1.0.1      # read .env

apscheduler==3.11.2          # scheduler for the realtime tiered pipeline

# dashboard
streamlit==1.39.0         # interactive dashboard UI
plotly==5.24.1            # premium charts

# dev/test
pytest==8.3.3
ruff==0.6.9

# NOTES:
# - Use Python 3.12. Check inside the venv with: python --version
# - Spark needs Java (8/11/17/21): java -version
# - On first streaming run Spark downloads the Kafka jar from Maven (internet ON).
